/**
 * Web re-export of shared product caps (popup + web same matrix).
 */
export {
  resolveProductCaps,
  resolveWebCaps,
  type ProductCapFlags,
  type ProductCaps,
  type ProductPlanLabel,
  type WebCapFlags,
  type WebCaps,
  type WebPlanLabel,
} from '@/shared/entitlement/resolveProductCaps';
