/**
 * @file i-messaging.ts
 * @description DEPRECATED — see ADR-004.
 *
 * Per ADR-004, all IPC goes through IMessageBus/ChromeMessageBus.
 * IMessaging, ITabQuery, and the Message interface are removed.
 * This file is kept as a tombstone to break transitive imports during
 * the staged migration; delete it in a follow-up cleanup.
 */

// Empty — content removed per ADR-004.
