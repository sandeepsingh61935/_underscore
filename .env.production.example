# Copy to .env.production for production web builds (or set in CI).
VITE_MCP_ENV=production
VITE_MCP_PACKAGE=@underscore/mcp-server
VITE_MCP_CLOUD_URL=https://underscore-mcp.sandeepss128961.workers.dev/mcp

# Cloudflare Pages Functions (ADR-027 LLM proxy) — set in Pages project env
# SUPABASE_URL=
# SUPABASE_ANON_KEY=

