// Fixture for category 4: legacy accent-tint scale (V2 keeps these, but the legacy
// 08/18/35/65 pattern in non-tokens.css files is what we catch)
// Expected violations: var(--accent-tint-08), var(--accent-tint-35)
export const a = 'var(--accent-tint-08)';
export const b = 'var(--accent-tint-35)';
